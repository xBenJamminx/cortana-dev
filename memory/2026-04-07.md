# 2026-04-07

## Openclaw Migration: Claude → Codex

Anthropic cut off Claude subscriptions from openclaw on 2026-04-04. Root cause was expired OAuth token compounded by the policy change — `claude-cli/sonnet` started returning `Unknown model` errors.

Fixed both servers:
- **Cortana** (5.78.181.172): migrated to `openai-codex/gpt-5.4-mini`, authenticated via benjoselson@gmail.com
- **MiMoo** (178.156.188.79): same migration

Config change in `/root/.openclaw/openclaw.json` on both servers:
- `agents.defaults.model.primary` = `openai-codex/gpt-5.4-mini`
- Removed stale `claude-cli/*` model entries

Also updated openclaw from `2026.2.24` → `2026.4.5` during this process (auto-updated).

## Claude Plan

Ben staying on Max 20x ($200/month). Only two tiers: 5x ($100) or 20x ($200). At 26% of 20x weekly usage from Claude Code alone, dropping to 5x would be too risky.

## Meeting Briefing Process Fixed

Cortana on GPT-5.4-mini was not running meeting briefings correctly:
- Was confusing meeting briefing with FAM sync
- Was using only the Fathom summary (19 min of a 111-min meeting) instead of the full transcript
- Was making up excuses about not having tools instead of using `client.py`

Fixed by:
- Updating server CLAUDE.md with explicit step-by-step process
- Updating local memory `feedback_meeting_briefing.md` with format rules
- Correcting Tram's placement (under Steven, not Ben)

## FAM Sync — April 7 (COMPLETED)

Ran full FAM sync from VS Code (Cortana on GPT couldn't execute it reliably).

**Notion:** 27 new tasks created (N-01 through N-27), all assigned correctly:
- Steven (Cao Tan Luc): N-01 to N-08, Firebase security task
- Bilal (Muhammad Bilal Akram): N-09 to N-15
- Ben: N-16 to N-25
- Cassandra: N-26 to N-27

**QA Sheet:** 12 rows updated (H column comments) with Apr 7 standup notes:
- Rows 31, 32 escalated with Monday Apr 13 hard gate
- Row 39 tagged with Cassandra's hard gate deadline
- Rows 42, 43 blocked on Unity upgrade
- Row 44 SQL fix ready (Steven to close)
- Row 48 confirmed in scope for demo

**Key discoveries:**
- Composio `GOOGLESHEETS_BATCH_UPDATE` is broken (ignores range, writes to A2)
- Use `GOOGLESHEETS_BATCH_UPDATE_VALUES_BY_DATA_FILTER` with `dataFilter.a1Range` for targeted cell writes
- Google OAuth token in server's `google_credentials.json` is expired (invalid_grant)
- Notion: use direct API with `NOTION_API_KEY_WORK` — Composio `NOTION_INSERT_ROW_DATABASE` ignores properties

## Pending

- Cortana's GPT behavior still degraded overall — she's more likely to hallucinate, make excuses, and not follow CLAUDE.md rules than Sonnet was. Worth monitoring and potentially tuning the system prompt further.
- Teach Cortana the correct Composio action names for future FAM syncs (document in context/fam-sync-process.md on server).
