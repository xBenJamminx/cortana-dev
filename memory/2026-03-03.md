# 2026-03-03 Memory

## Sub-Agent System — Root Cause Found and Fixed

**Problem:** spawn_task.sh used  which routes to Cortana's live
session. Sub-agent inherited full conversation history + orchestrator rules → saw long task →
followed CLAUDE.md delegate long tasks rule → spawned another sub-agent → infinite recursion.
Nothing ever got done. Workers were silently looping.

**Fix:**
- Added  agent to /root/.openclaw/openclaw.json
- spawn_task.sh now uses 
- Each worker starts clean, isolated from Cortana's session
- Anti-recursion preamble in every worker task message
- Verified: ISOLATED_CONFIRMED (worker has no Cortana history)
- Verified: worker executes directly, reports to Telegram, does not re-delegate

**Also fixed:** tg-reaction-monitor was broadcasting once per model retry → spam across all topics.
Now debounced: one alert per incident. Also killed duplicate monitor process.

## Google Calendar Integration
- gcal.py: /root/.openclaw/workspace/lib/gcal.py
- Composio GOOGLECALENDAR_CREATE_EVENT injects owner as attendee (triggers confirmation prompt)
- Fix: create event → immediately PATCH with attendees:[] to remove them
- DO NOT pass send_updates to PATCH_EVENT (expects string, not bool — errors silently)
- Working connection: COMPOSIO_GCAL_CONNECTION_ID=49bd9332-c345-4ae8-a832-e046ec8835d0

## MiMoo Updates
- Same sub-agent pattern deployed to mimoo server (178.156.188.79)
- gcal.py adapted for multi-tenant (uses COMPOSIO_ENTITY_ID, no hardcoded connection IDs)
- GitHub: xBenJamminx/mimoo — commits fa48d79 (gcal) + 865b1c3 (spawn_task)
- AGENTS.md, TOOLS.md, skills/composio/SKILL.md all updated

## Files Modified Today
- /root/.openclaw/openclaw.json — worker agent added
- /root/.openclaw/workspace/lib/spawn_task.sh — rewritten for worker agent
- /root/.openclaw/workspace/scripts/tg-reaction-monitor.py — debounce fix
- /root/.openclaw/workspace/BRAIN.md — updated with all above
